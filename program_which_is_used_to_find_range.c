#include <stdio.h>
#include <float.h>
#include <limits.h>
int main()
{ 
 
 printf("min range of int is %e \n", INT_MIN);
 printf("max range of int is %e \n", INT_MAX);
 printf("min range of SHORT is %e \n", SHRT_MIN);
 printf("max range of SHORT is %e \n", SHRT_MAX);
 printf("min range of FLOAT is %e \n", FLT_MIN);
 printf("max range of FLOAT is %e \n", FLT_MAX);
 printf("min range of CHAR is %e \n", CHAR_MIN);
 printf("max range of CHAR is %e \n", CHAR_MAX);
 return 0;
}
